mutation Register($options: RegisterInput!) {
  register(options: $options) {
    instructor {
      email
      createdAt
      updatedAt
      id
      firstName
      lastName
      instructorTitle
      description
    }
    errors {
      field
      message
    }
  }
}