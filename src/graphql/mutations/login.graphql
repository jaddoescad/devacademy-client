mutation Login($options: LoginInput!) {
  login(options: $options) {
    errors {
      field
      message
    }
    instructor {
      email
      createdAt
      updatedAt
      id
      firstName
      lastName
      instructorTitle
      description
    }
  }
}
